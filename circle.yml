machine:
  environment:
    USER_NAME: bugtender
    USER_EMAIL: bugtender@gmail.com

general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

deployment:
  production:
    branch: master
    commands:
      - chmod +x ./automata
      - ./automata
